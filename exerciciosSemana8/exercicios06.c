#include <stdio.h>
/*
int main()
{
    char nomes[5][20], pesquisar[20], nomeachado;
    ;

    for (int i = 0; i < 5; i++)
    {
        printf("Digite os nomes das pessoas: ");
        scanf("%s", nomes[i]);
    }

    for (int i = 0; i < 5; i++)
    {
        printf("%s\n", nomes[i]);
    }

    printf("Digite um nome para voce pesquisar no vetor: ");
    scanf("%s", pesquisar);

    int i = 0, j = 0;
    while (nomes[i][j] != '\0' || pesquisar[j != '\0'])
    {

        for (; j < 5; j++)
        {
            if ()
            {
*/
